const admin = require('firebase-admin');
const fs = require('fs');

// The project does not have a firebase-admin dependency but we can use the web SDK in a simple node script? No.
// Let's use tsx and the firebase web sdk.
